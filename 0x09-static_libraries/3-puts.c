#include<stdio.h>
#include "main.h"
/**
 * _puts - String to the output
 * @str: input value
 *
 * Return: Always 0 (Success)
 */
void _puts(char *str)
/* main - Entry point */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (s);
}
