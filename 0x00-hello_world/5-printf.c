#nclude <stdio.h>

/*
 *main - Print a string
 *Description: use printf
 *Return: 1
 */

int main(void)
{
		printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
			return (1);
}
